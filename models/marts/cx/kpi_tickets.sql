{{ config(enabled=false) }}

-- Placeholder intencional.
-- A procedure CX.GOLD.KPI_TICKETS() é chamada no pipeline legado,
-- mas a definição não foi fornecida nos scripts de origem.
-- Modelar este mart sem a lógica fonte seria inventar regra de negócio.

select 1 as placeholder
